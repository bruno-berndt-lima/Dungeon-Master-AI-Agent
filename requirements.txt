# Pinned 2026-07-31, re-trimmed 2026-09-18. Requires Python >=3.11 (see pyproject.toml).
#
# These pin the LangChain/LangGraph 1.x line. The codebase was written against
# 0.x (Feb 2025) and migrated in PR-01..PR-08; the suite runs green on these
# exact versions under Python 3.12 and 3.13.

# --- orchestration -----------------------------------------------------------
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1   # SqliteSaver, for campaign state across runs
langchain-core==1.6.3
langchain-text-splitters==1.1.2  # was pulled in by the `langchain` meta package, now pinned directly
# Removed in PR-11: `langchain` (nothing imported it — only langchain_core,
# langchain_text_splitters, langchain_ollama and langchain_chroma are used) and
# `langchain-community` (sunset upstream; its one use, PyMuPDFLoader, is now
# ten lines over pymupdf in src/data/loader.py).

# --- model providers ---------------------------------------------------------
langchain-ollama==1.1.0          # chat, structured output, tool binding

# --- retrieval ---------------------------------------------------------------
langchain-chroma==1.1.0
chromadb==1.5.9
# Embeddings go through the Ollama daemon too (`OllamaEmbeddings`), so there
# is no torch / sentence-transformers / transformers here any more. That block
# used to pin the whole Intel-macOS stack (torch 2.2.2 → cp312 → Python 3.12
# exactly → numpy 1.x); PR-12 removed it. Pull the model once:
#     ollama pull all-minilm
pymupdf==1.28.0

# --- dev ---------------------------------------------------------------------
pytest==9.1.1

# Removed in PR-01:
#   langchain      (was listed twice)
#   langchainhub   (only reachable via the unused src/pipelines/generator.py)
#   tavily-python  (never imported)
